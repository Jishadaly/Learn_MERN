import React from 'react'
import withAuth from '../HOC/withAuth'

const  Dashboard=()=> {
    return (
        <div>
            this is dashboard
        </div>
    )
}

export default withAuth(Dashboard);
